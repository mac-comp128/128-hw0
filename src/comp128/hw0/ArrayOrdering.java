package comp128.hw0;

public class ArrayOrdering {

    /**
     * Calculates the subset of array elements from numbers that are larger than all elements
     * to their right in the array. Returns a new array with each element that is smaller zeroed out.
     * E.g. for {13, 3, 8, 5, 4, 7}, it should return a new array of {13, 0, 8, 0, 0 7}.
     * @param numbers Input. Should not be modified
     * @return New array with smaller elements zeroed out.
     */
    public static int[] getLargerLeftElements(int[] numbers) {
        // TODO: replace me with the correct algorithm and return statement
        return numbers;
    }
}
